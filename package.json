{
  "name": "truyendrive-cli",
  "version": "0.2.1",
  "description": "Encrypt image folders into a sibling truyendrive directory using XOR noise.",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/cli.js",
  "bin": {
    "truyendrive-cli": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run build",
    "test": "vitest run"
  },
  "keywords": [
    "cli",
    "image",
    "encryption",
    "sharp"
  ],
  "dependencies": {
    "commander": "^14.0.0",
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
